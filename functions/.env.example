# Nicht-geheime Konfiguration (wird beim Deploy hochgeladen).
# Secrets (DISCORD_CLIENT_SECRET, BACKUP_SIGNING_KEY) NICHT hier eintragen,
# sondern: firebase functions:secrets:set <NAME>
DISCORD_CLIENT_ID=
DISCORD_REDIRECT_URI=

# DigitalOcean Spaces (optional — Datei-Uploads/-Downloads)
# DO_ENDPOINT=
# DO_BUCKET_NAME=
# DO_PUBLIC_URL=
