name: Build & Release Windows App

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Check out git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build React App
        run: npm run build
        env:
          REACT_APP_API_BASE_URL: ${{ secrets.REACT_APP_API_BASE_URL }}
          REACT_APP_FIREBASE_API_KEY: ${{ secrets.REACT_APP_FIREBASE_API_KEY }}
          REACT_APP_FIREBASE_AUTH_DOMAIN: ${{ secrets.REACT_APP_FIREBASE_AUTH_DOMAIN }}
          REACT_APP_FIREBASE_PROJECT_ID: ${{ secrets.REACT_APP_FIREBASE_PROJECT_ID }}
          REACT_APP_FIREBASE_STORAGE_BUCKET: ${{ secrets.REACT_APP_FIREBASE_STORAGE_BUCKET }}
          REACT_APP_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.REACT_APP_FIREBASE_MESSAGING_SENDER_ID }}
          REACT_APP_FIREBASE_APP_ID: ${{ secrets.REACT_APP_FIREBASE_APP_ID }}
          REACT_APP_FIREBASE_MEASUREMENT_ID: ${{ secrets.REACT_APP_FIREBASE_MEASUREMENT_ID }}

      - name: Build and release Electron app
        uses: electron-userland/electron-builder-action@v1
        with:
          release: true
        env:
          # ############################################################### #
          # ## HIER IST DER PLATZHALTER FÜR DEINEN TOKEN.                ## #
          # ## DIESER CODE BLEIBT GENAU SO, WIE ER IST.                  ## #
          # ## ÄNDERE IHN NICHT!                                         ## #
          # ## Füge deinen echten Token NUR in den GitHub-Einstellungen  ## #
          # ## unter dem Namen GH_TOKEN ein.                             ## #
          # ############################################################### #
          GH_TOKEN: ${{ secrets.GH_TOKEN }}